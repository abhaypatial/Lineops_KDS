{
  "name": "@workspace/scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "release": "tsx ./src/release.ts",
    "export-docker": "tsx ./src/export-docker.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
